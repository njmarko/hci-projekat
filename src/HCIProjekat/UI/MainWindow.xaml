<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"      
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label Content="{Binding Name}" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Go to login" Command="{Binding Context.Router.RouterPushCommand}" CommandParameter="Login" />
                <Button Content="Go to clients requests" Command="{Binding Context.Router.RouterPushCommand}" CommandParameter="ClientRequests" />
            </StackPanel>
            <ContentControl Content="{Binding Context.Router.CurrentViewModel}"></ContentControl>
        </StackPanel>
    </Grid>
</Window>
