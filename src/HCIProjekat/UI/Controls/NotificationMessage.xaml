<UserControl x:Class="UI.Controls.NotificationMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="b2vc"></BooleanToVisibilityConverter>
    </UserControl.Resources>
    <Grid Margin="0" Width="200" Height="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="1" HorizontalAlignment="Right" Text="{Binding TimeStamp}" Foreground="Gray" />
        <materialDesign:PackIcon Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Kind="CheckAll" Foreground="Gray" Visibility="{Binding IsRead, Converter={StaticResource b2vc}}" />
        <materialDesign:PackIcon Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Kind="NotificationsActive" Foreground="Gray" Visibility="{Binding IsNew, Converter={StaticResource b2vc}}" />
        <materialDesign:PackIcon Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" Kind="Check" Foreground="Gray" Visibility="{Binding IsSeen, Converter={StaticResource b2vc}}" />
        <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Message}" TextAlignment="Center" HorizontalAlignment="Center" TextWrapping="WrapWithOverflow" />
    </Grid>
</UserControl>
